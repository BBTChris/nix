# ARC 038 sub-agent D — CHECK-DEBT rows, ready to paste

Reserved id block: **D3.378 – D3.387**. **All ten used (D3.378–D3.387).**

**Vocabulary note for the integrator:** the shared audit contract says the
`owning module` token *"is almost always `risk`"*. There is **no `risk` token** in
`docs/CHECK-DEBT.md`'s controlled vocabulary — the token for `scripts/nixrisk/` is
**`limiter`**, and an unknown token is a loud `ProbeError`, not a silent exclusion.
Rows below use `limiter` (for `scripts/nixrisk/`) and `verify` (for
`checks/*.py` and test-harness subjects), read off the vocabulary table at
`docs/CHECK-DEBT.md:349-364`. Verified on-disk state outranks the brief (directive 5).

| D3.378 | **`PictureMirror` still trusts a FUTURE `published_ts` forever, and the bound it needs is §12A's `CLOCK_SKEW_MAX_MS`** | ARC 038 (Stage 1 / sub-agent D), MEASURED | The NON-FINITE half of FD3 is discharged this arc (`published_ts` added to `picture_defects`' `_finite` sweep, both halves proven by `test_arc038_d_money_truth.py::test_FD3_a_NON_FINITE_FRESHNESS_STAMP_cannot_make_a_mirror_ETERNALLY_TRADABLE`). What remains is the FINITE-but-impossible stamp: a picture stamped 3,000 years ahead gives `age = clock() - published_ts = -93212904555.156 s`, `age > max_age_s` is False, and `PictureMirror.tradable()` returned `(True, "picture version 2, age -93212904555.156s")` — measured. The staleness arm is one-sided by construction: it bounds how OLD a stamp may be and not how far into the future it may sit, and `FinancialPictureBook._build` takes an injected `clock`, so a skewed or wrongly-injected publisher clock produces it without any impostor. Discharge = a bound on negative age keyed to §12A's `CLOCK_SKEW_MAX_MS` (250 ms in `risks/staleness.config.json`, which `check_staleness` already loads), plus an arm on `check_picture_atomicity._arm_stale` driving a future stamp. **NOT minted here on purpose:** the number is a semantic decision §12A owns, and `AggregateMarginCapRule`, `FinancialPictureBook` and `SurvivalWatch` all refuse a default for exactly this reason — a ceiling with a default is a ceiling nobody chose. Coupled to D3.125, which records that `max_age_s` itself has no §12A home for either mirror | ARC 039 | limiter |
| D3.379 | **§3's financial picture carries NO WRITER IDENTITY on the wire, and a second process injected a fabricated one: balance $10,000 -> $10,000,000, deployable $500 -> $7,000,000** | ARC 038 (Stage 1 / sub-agent D), MEASURED end to end on this node | Drive: a real `StatePublisher` binds `ipc://`, publishes a true picture (balance 10,000, one OPEN row at 6,500 margin, `deployable` 500); a consumer's `PictureMirror` completes and reports `(True, 'picture version 2, age 0.022s')`. The real writer then dies. A SECOND `StatePublisher` binds the SAME path — D3.316 already measured that `ipc://` bind is NOT exclusive, libzmq unlinks and rebinds with no `EADDRINUSE` — and publishes `balance=10_000_000.0` with an empty table, calling `refresh_all()`. The subscriber auto-reconnects and the mirror ADOPTS it: `v=41 balance=10000000.0 committed=0.0 deployable=7000000.0`, `tradable() -> (True, 'picture version 41, age 0.041s')`, `picture_defects: ()`, `out_of_order dropped: 0`. **The asymmetry is the finding:** `nixscore`'s ranking table — the one table §6.6:467 says must NEVER gate safety — HAS a `writer_identity` check that D3.316 calls *"the whole of §6.6's sole-writer enforcement"*, while §3's financial picture, which every Phase-B money rule reads through `current()`/the mirror, has none. `picture.py`'s own docstring claims *"Single writer by construction … there is no verb by which a consumer can write back"*, which is true in-process and false across the boundary. Neither `check_picture_atomicity` (one process, and it says so) nor `check_state_bus` (asks whether delivery works, not who bound the socket) can see it, and both are GREEN. Discharge = port `nixscore`'s pattern: a `writer_identity` field on the body (`WIRE_SCHEMA` 2 -> 3), a CONFIGURED expected identity on the consumer with a §12A home, refusal counted as `foreign_rejected` is in `nixscore`, and an arm on `check_picture_atomicity` driving the hijack across two real processes. **NOT done in this arc's freeze:** it is a schema bump plus a new knob plus re-pointing two of the gate's arms | ARC 039 | limiter |
| D3.380 | **`PictureMirror` keys freshness on AGE ALONE — 22,356 `tradable()` permissions over 0.477 s from a SIGKILLed publisher, while the tree's own liveness observer on the SAME socket already said the writer was gone** | ARC 038 (Stage 1 / sub-agent D), MEASURED on this node | Publisher in a CHILD PROCESS over a real `ipc://` endpoint; parent holds `PictureMirror(max_age_s=0.5)` and, purely as an observer, a `nixscore.liveness.PublisherLiveness` on the same subscriber socket. `os.kill(child.pid, SIGKILL)` from the parent, reaped `rc=-9` (asserted). Then: **22,356 `tradable()` verdicts permitted sizing over 0.477 s**, first refusal at +0.478 s and by the AGE route only (*"is 0.500s old, over the 0.500s ceiling"*), `headroom off the corpse: deployable=65991.7 committed=4009.0 balance=100001.0`. At the same instant the observer read `peer_observed=False` with `signal='peer'`. `tradable()`'s four refusal arms are no-snapshot / undecodable / self-inconsistent / over-age; there is no liveness input and no `note_liveness` verb. This is **D3.244's exact shape transposed onto the table that gates money** — D3.244 measured 140,830 ranked decisions from a corpse over 0.476 s on the ranking table, was HALF-discharged in ARC 037 by wiring `PublisherLiveness` into `nixscore`, and D3.310 records that the discharge condition was stated backwards: the age was standing in for an observation nobody was making. Same substitution here, one module over, and the observation is already available on the same file descriptor. It scales linearly with `max_age_s`, which D3.125 records has no §12A home. **Deliberately NOT repaired unilaterally:** D3.123 is an OPEN architect ruling on whether `PictureMirror` should exist at all (it records that `nixalloc.mirror.AllocatorMirror` subsumes it, same codec, same ceiling, plus the state enumeration and the §6.4b guard), and D3.319 records that the liveness stance's DEFAULT is itself a decision with a measured downside either way. Wiring a signal into a class the ledger says may be retired builds on a slab under review. Discharge = D3.123's ruling, then the observer wired into whichever mirror survives, with `check_picture_atomicity` gaining a kill arm in both directions (a window near zero would mean the trigger is liveness, an unbounded one that the transition never happens — D3.310's lesson) | ARC 039 | limiter |
| D3.381 | **The frozen seam's `BrokerTruth` carries ONE unlabelled `balance` — the "single equity figure" §15 C2 exists to forbid — and nothing in production feeds the financial picture at all, so the conflation cannot be measured either way** | ARC 038 (Stage 1 / sub-agent D), MEASURED as an ABSENCE and stated as one | I6's discrimination RESISTED everywhere the two quantities are USED: an AST census found 119 cash-like/net-liq-like reads across 12 files, only three files read both families, and driving cash 100,000 / net-liq 40,000 and the inverse with the floor placed BETWEEN them moved every sizing output with `balance`/cash and every survival verdict with net-liq, each invariant in the other. What could NOT be measured is the WIRING. `grep -rn "FinancialPictureBook(" scripts/ --include=*.py` filtered of `scripts/tests/` returns **nothing**: the §3 publisher has zero production construction sites, so "is the figure fed to `balance` the CASH one?" has no production answer. The one production writer that supplies a balance is correct (`flatten.py:674 balance=balance.cash`, with `flatten.py:194-196` declaring `cash` and `net_liquidation` separately and citing §15 C2 on each). Against that, `seam.py:806-819`'s `BrokerTruth` — the cold-start truth type, FROZEN — has a single `balance: float` with cash-or-net-liq unstated, and `coldstart.py:942` consumes it; and `scripts/tests/test_broker_order.py:3000`'s FINDING F-A8-1 records that the broker's PUSH path delivers `Balance.cash == 0.0` while the query path delivers the real figure, so the cash side of the seam is already known to be unreliable on one route. **A conflation introduced at the wiring seam would be invisible to every gate in this set**, because all of them take the picture as a given. Discharge = the arc that builds the Limiter's run loop (or cold-start's broker wiring) names which figure fills `BrokerTruth.balance`, ideally by splitting it as `SurvivalWatch.BrokerReading` already does (`cash` and `net_liq` as two fields, one poll), recorded as a `SPEC-A<n>` if the frozen seam must widen; plus a gate arm that drives cash and net-liq apart THROUGH the wiring rather than through the classes | ARC 039+ | limiter |
| D3.382 | **`gate._largest_fit`'s guard is `margin_per_contract <= 0.0`, which a NaN passes — and the Allocator's twin of the same clamp checks `math.isfinite`** | ARC 038 (Stage 1 / sub-agent D), MEASURED | `_largest_fit(room, nan, True)`: `nan <= 0.0` is False, so it proceeds to `int(room // nan)` = `int(nan)` and raises `ValueError: cannot convert float NaN to integer`. Driven with ±inf room as well, same raise. `order.margin_per_contract` is an Allocator-supplied field that crosses the wire, so the input is not the Limiter's own. **It is CONTAINED and the containment was verified, not assumed:** `GatePass._dispatch` catches `Exception` broadly and returns a DENY naming the rule and the exception text (*"rule raised ValueError: … — a rule that cannot answer has not approved"*), so the outcome is fail-CLOSED and §18's reason requirement is met. The finding is the ASYMMETRY: `nixalloc.sizing.margin_contracts` and `risk_contracts` both guard `if mpc <= 0.0 or not math.isfinite(mpc): return 0`, and `gate._largest_fit` — the Limiter's authoritative re-computation of the same clamp — does not, so two implementations of §7:483's clamp disagree on the same input. Not repaired here: `scripts/nixrisk/gate.py` is another sub-agent's subject in ARC 038 and I did not touch it (frozen SHA `26ed1983…` unchanged). Discharge = one `math.isfinite` in `_largest_fit`'s guard plus an arm on `check_limiter_gate` driving a non-finite `margin_per_contract` and requiring the DENY to name the FIELD rather than the exception | ARC 039 | limiter |
| D3.383 | **A plant/restore harness that restores with `git checkout --` silently DESTROYS the arc's own uncommitted repairs — measured, in this audit, on two frozen files** | ARC 038 (Stage 1 / sub-agent D), MEASURED the hard way | My first gate-plant harness planted a violation in `scripts/nixrisk/picture.py` and `scripts/nixrisk/survival.py`, ran the gate (both went RED and named their sites — the audit worked), then restored with `subprocess.run(["git", "checkout", "--", rel])`. That reverted each file to HEAD and **deleted this arc's four repairs**, which were uncommitted at the time; the harness's own sha-before/sha-after comparison caught it (`byte-identical=False`) only because I had printed both. A restore that reverts to the INDEX cannot distinguish *the plant* from *every change since the last commit*, and the failure is silent in the common case where the agent does not diff. This is the same class as D3.205/D3.344 — a tool reaching state the operator did not name — one layer over: not an inherited environment variable re-pointing a subprocess, but an inherited COMMIT re-pointing a restore. Repaired in my harness (in-memory `read_bytes()` snapshot, restore by `write_bytes`, SHA-256 asserted byte-identical) and the repairs were committed BEFORE re-driving, which is the second half of the lesson. **The class stays open:** nothing in the tree enumerates which plant harnesses restore from git and which from a snapshot, and `checks/` contains several that stage or mutate. Discharge = a shared plant/restore helper in `scripts/nixverify/` that snapshots bytes and asserts byte-identity, plus a sweep naming every harness that does not use it | ARC 039+ | verify |
| D3.384 | **`check_picture_atomicity` is GREEN over FOUR resident defects in its own declared subject, and it is not vacuous — it is scoped past them** | ARC 038 (Stage 1 / sub-agent D), MEASURED | The gate is a strong instrument: `SUBJECTS = ("scripts/nixrisk/picture.py",)`, 2,000 reads across 69 versions with the writer at generation 201, 40,128 real wire bytes, three PLANTS tearing at 100.0% / 3.6% / 1.2% (the last on the `stop_distance` axis), and floors that return CANNOT_MEASURE rather than pass. Planting §3's coherent tear inside the subject (`_next`: `balance=previous.balance`) reddens it, naming the version, the row and both generations. It was nevertheless GREEN over **FD1** (a refused publish advanced `_current`, and the poisoned table made the full `GatePass` APPROVE 800 contracts / $400,000 on a $10,000 account), **FD3** (`published_ts` unvalidated ⇒ a permanently-tradable mirror), **FD4** (`OverflowError` out of `tradable()`, documented *"Never raises"*) and **FD6** (22,356 permissions from a corpse). The mechanism in each case is scope, not vacuity: no arm ever drives a REFUSED commit, so `refusals` is 0 on every run and the poisoned branch is never entered; the stale arm drives three FINITE stamps; the codec arm drives a MISSING key and a wrong `schema`, never a value whose type is right and magnitude is not; and the docstring is honest that the gate runs one process. All four are now gated by `scripts/tests/test_arc038_d_money_truth.py` (14 controls, each with its unprotected half), which is where doctrine C.9 puts one property's one instrument — so this row is about the GATE's reach, not about an ungated property. Discharge = either three new arms on this gate (a refused-commit arm asserting `current()` is unchanged, a non-finite / future-stamp arm, an `Infinity`-on-the-wire arm) with the suite's duplicates retired, or a recorded ruling that the pytest suite is the right home and the gate's declared scope is amended to say so — **not both**, per C.9 | ARC 039 | verify |
| D3.385 | **§12.7's RESTART REBUILD never reaches an already-connected consumer: all 60 rebuild snapshots were DROPPED as out-of-order, and the mirror kept asserting an OPEN position the "restart = flat" invariant denies** | ARC 038 (Stage 1 / sub-agent D), MEASURED on this node | `statebus.Mirror.apply` orders by the TRANSPORT sequence (`if message.seq <= previous: self.out_of_order += 1; return`), and `StatePublisher.__init__` starts every instance at `self._seq = 0`. So the ordering key is per-publisher-INSTANCE, not per-table. Drive: publisher A binds a real `ipc://` endpoint, a consumer's `PictureMirror` completes, A runs 399 more commits (transport `_seq` -> 400). A closes — the Limiter restarts. Publisher B binds the same path and publishes §12.7's rebuild (post-restart truth: balance 99,999, FLAT per §14:970), calling `refresh_all()` sixty times so the snapshot flag is set every time. Result: **mirror still v=400, balance=10000.0, sum_open_margin=899.0, `out_of_order` DROPPED=60**, `tradable() -> (True, 'picture version 400, age 4.755s')`. The mirror can NEVER apply another message from this publisher; its only exit is ageing out. §12.7:658's *"a restarted consumer is correct within seconds — it never waits for organic deltas"* is defeated in the publisher-restart direction entirely, and inside the freshness ceiling the consumer sizes on a picture asserting the exact state §14:970's *"Restart = flat, always"* guarantees cannot exist. **It does end fail-closed** (the stamp ages out), which is why this is a debt row and not a HALT-class defect. `check_state_bus`'s arms are delivery / late subscriber / withheld-`service()` control / delta-only-is-stale / freshness stamp — no arm restarts a publisher under a live subscriber, and `check_picture_atomicity._late_subscribers` stages two subscribers against ONE publisher, the opposite direction. `Mirror.out_of_order` exists so this is not hidden and nothing outside the mirror reads it. **Discharge is COUPLED to D3.379 and must not be attempted alone:** telling a legitimate NEW writer from a stale duplicate IS the writer-identity question, and a sequence guard cannot answer it — accepting any `_kind == "snapshot"` regardless of seq would close this row and open D3.379 wider, because an impostor's snapshot would then always win. So: one repair, `writer_identity` on the body, and the seq guard scoped per (topic, writer) with a new identity resetting it; plus an arm on `check_state_bus` restarting a publisher under a live subscriber and requiring the rebuild to LAND, with the impostor control requiring it not to | ARC 039 | capture |
| D3.386 | **`FinancialPictureBook.commit` releases the `_writing` guard BEFORE `publish`, so a re-entrant sink is not refused and the wire can carry picture versions OUT OF ORDER — which the mirror's transport-seq guard cannot discard** | ARC 038 (Stage 1 / sub-agent D), MEASURED, and NOT currently reachable in this tree | `commit()`'s `try/finally` sets `self._writing = False` and only then calls `self.publish(picture)`, so the sink runs OUTSIDE the single-writer guard. Driven with a sink whose `emit` calls back into `commit`: no `ConcurrentWriter` was raised, and the wire received **`[3, 2]`** — the inner commit's version 3 published before the outer's version 2. The mirror orders by the transport `_seq`, which is monotone by construction and therefore rises across both sends, so it CANNOT discard the regression: it applies v3 then v2 and ends holding **v2 while the book holds v3**, with `out_of_order == 0` and `picture_defects()` empty on both. Nothing observes that the published version stream is non-monotone. **Reachability, stated honestly: no sink in this tree re-enters.** `StateBusPictureSink.emit` calls `StatePublisher.publish` -> `send_multipart` and returns; the fan-outs in `flatten.py` and `recovery.py` run after `commit` returns, not from inside it. So this is a representable defect with no live caller, recorded rather than repaired because the repair is a real behavioural choice (either move `publish` inside the guard — which then makes a transport failure leave `_current` behind, a fail-OPEN under-count of commitments — or add a version-monotonicity assertion at the sink boundary, which is the smaller change and the one I would take). Related to D3.379/D3.385: all three are the mirror trusting an ordering key that does not order what the consumer cares about. Discharge = the ruling on which of the two repairs, plus an arm requiring a re-entrant sink to be REFUSED by name | ARC 039+ | limiter |
| D3.387 | **The measured $400,000 fail-open in FD1 used an input the shipped reservation ledger refuses — the structural defect is repaired, the reachability of its WORST case is unproven, and no gate would notice a caller that reached it** | ARC 038 (Stage 1 / sub-agent D), MEASURED, and recorded because the honest version of a finding is worth more than the headline | FD1 is discharged: `FinancialPictureBook.commit` no longer advances `_current` on a refused publish, and `test_arc038_d_money_truth.py` proves both halves. This row records what the DRIVE does and does not establish. The full fail-open — the §3 `GatePass` approving 800 contracts / $400,000 of margin on a $10,000 account — was reached with `commit(sum_reservations=float("-inf"))`, and `scripts/nixrisk/reservations.py:300` guards `if not math.isfinite(margin) or margin < MIN_MARGIN: raise InvalidReservation`, so `ReservationLedger.total_reserved()` cannot produce it. The reachable refusals reach weaker places: a duplicate `trade_id` poisons in the CONSERVATIVE direction (`sum_open_margin` 500 -> 1000, `deployable` 6500 -> 6000) while still leaving the own table and the mirror at different versions, and a NaN `balance` clamps `deployable` to 0.0 and makes `gate._largest_fit` raise, which `GatePass._dispatch` converts to a DENY naming the rule (fail-CLOSED). **The gap this row keeps open is not the repaired ordering, it is the SEAM:** `commit()`'s `sum_reservations` parameter is a bare `float` and the finiteness guard that makes the worst case unreachable lives in a DIFFERENT class, with nothing connecting the two — so a second caller (the §11.3 running-aggregate path, a cold-start rebuild, a drift-audit correction) can supply the figure without passing through `ReservationLedger.take`, and no gate enumerates who may. Discharge = either `commit()` refusing a non-finite input at its own boundary with a reason naming the parameter (which the FD1 repair now does as a side effect, via `picture_defects` — so what is really owed is a gate arm ENUMERATING the callers of `commit()` and requiring each to source `sum_reservations` from the ledger), or a recorded ruling that `FinancialPicturePort` is a trusted-caller surface. Note this row is about a REPAIRED defect's residual and should be closed by measurement, not by re-reading FD1 | ARC 039 | limiter |
