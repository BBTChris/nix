# ARC 038 sub-agent A — CHECK-DEBT rows (ready to paste)

Reserved block D3.348 – D3.357. Seven used: **D3.348 – D3.354**. D3.355 – D3.357 unused.

Owning-module token: **`limiter`**, read out of `docs/CHECK-DEBT.md`'s "The controlled
vocabulary" section on this tree. (The brief said the token would "almost always be
`risk`"; there is no `risk` token in the vocabulary and an unknown token is a loud
`ProbeError`, so the on-disk vocabulary wins — directive 5.)

Paste into the D3 table in id order.

| D3.348 | **`RulePort.phase` is a PROPERTY with no stability contract, and the frozen seam cannot give it one** | ARC 038 (sub-agent A, FA-1), MEASURED | `nixrisk/seam.py:159` declares `phase` as a `@property` returning `Phase`, and its docstring says only *"Declared, never inferred."* Nothing says it must answer the same member twice. `GatePass` read it three times — once in `_validate`, once per partition comprehension — and a rule answering `A, B, A` across those reads was DROPPED FROM BOTH PARTITIONS: measured on the shipped `default_manifest` plus one always-DENYING extra rule, nine rules dispatched from a ten-rule manifest, the extra rule never evaluated, the pass APPROVED and the reservation TAKEN. The mirror sequence `A, A, B` put it in BOTH and dispatched it TWICE inside what §3:113 calls ONE authoritative pass — eleven names in `GateOutcome.evaluated` for ten rules. **The subject half is DISCHARGED in this arc**: `_validate` now RETURNS the phase it read and `__init__` partitions from that tuple, so there is exactly one read and the reads cannot disagree; the standing gate is `check_limiter_gate.arm_partition_is_a_partition`, proven to redden on the restored three-read partition and to name `scripts/nixrisk/gate.py:GatePass.__init__[partition]`. **What is NOT discharged is the seam:** the port still permits an unstable declaration, so any FUTURE consumer that reads `rule.phase` more than once rebuilds this defect, and `seam.py` is FROZEN so the contract cannot be tightened where it belongs. Discharge = a `SPEC-A<n>`-style amendment adding the stability sentence to `RulePort.phase`, or a seam-side unfreeze arc | ARC 039+ | limiter |
| D3.349 | **NOTHING IN PRODUCTION CONSTRUCTS A `GatePass`, A `HaltFlag`, A `BlackoutEvaluator` OR A `ProtectiveFlatten` — the §3:173 onset sweep this arc wired is a correct mechanism in an object graph nothing roots** | ARC 038 (sub-agent A, FA-2), MEASURED by AST over `scripts/` minus `scripts/tests/` | `grep -rn "GatePass(\|HaltFlag(\|BlackoutEvaluator(\|ProtectiveFlatten("` over non-test `scripts/` returns THREE sites, all `GatePass(`, all inside Plane-1 drills: `scripts/plane1_hotpath_drill.py:235`, `scripts/plane1_degraded_drill.py:862,1112`. Zero for the other three classes. So every measurement of the Limiter's behaviour in this arc — including the blackout-onset sweep FA-2 added and the partial-sweep reporting FA-3 added — is of a library composed by an instrument, and the wiring that would make it fire in production does not exist. This is the same bound `halt.py`'s `PRODUCERS` map already states one layer down (*"no daemon in `scripts/` constructs a `HaltFlag` or a `DriftAudit` at all, so a call site here is a call site inside an object graph nothing roots"*), restated here because the onset sweep is the FIRST safety action whose absence was a measured VIOLATION rather than an unbuilt feature. A related shape from the same drive: `scripts/plane1_hotpath_drill.py:295,351,388,483` call `gate.evaluate(...)` and **discard the return value** — harmless only because those call sites place no order. Discharge = the Limiter daemon / §5 event loop that constructs the object graph, which is a build arc and not an ULTRAREVIEW | ARC 039+ | limiter |
| D3.350 | **Nothing re-attempts a cancel the broker REFUSED, and a second HALT cause does not re-sweep — an entry that survives one onset stays live at the venue until an operator reads a field** | ARC 038 (sub-agent A, FA-3), MEASURED | With FA-3 repaired the sweep COMPLETES and names what it could not cancel (`OnsetCancellation.failures`, a per-entry Plane-1 `CANCEL` row, and `onset_sweep=partial` on the §12.10:753 `halt_set` row). Nothing then does anything about it. Measured: three working entries, `cancel_order("c-2")` raising `BrokerNotConnected`, `HaltFlag.set` → `sweep=partial`, and `c-2` still on the venue's working book and still able to FILL — inside a HALT — for two contracts. The second half of the same drive: `HaltFlag.set` sweeps on the 0→1 EDGE ONLY, so a second cause arriving while HALT is already set does not re-sweep (`2nd cause: booked=True sweep=skipped working=['c-late']`). The module's argument for edge-only is that branch 0 is already denying new entries, which is true of entries created THROUGH THE GATE and false of two others: an ack that was in flight when the sweep ran, and an entry a refused cancel left behind. §3:174 is *"no order may fill inside a window it was not approved for"*, and both cases can. Discharge = a pending-entry reaper on the Limiter's §5 loop that re-attempts a `partial` sweep until the venue's working set for the symbol is empty or an operator is alerted (§12.9) — it needs the loop D3.349 names | ARC 039+ | limiter |
| D3.351 | **`GatePass`'s `ledger` argument is OPTIONAL, so a gate that APPROVES and reserves NOTHING is constructible, and one is constructed in this tree** | ARC 038 (sub-agent A, FA-4), MEASURED | `gate.py:GatePass.__init__(self, halt, rules, ledger: ReservationLedgerPort \| None = None)`. §3:150 is *"approve ⇒ TAKE RESERVATION (proposed margin)"* with no conditional, and `_settle` honours it only `if self._ledger is not None`. `scripts/plane1_hotpath_drill.py:235` constructs `GatePass(clear, list(rules))` — no ledger — so the shipped tree already contains a pass that approves and commits nothing. It surfaced during FA-4: with no ledger a `qty=-5` proposal was APPROVED with no reservation and no complaint, and the only reason the ledger-wired path denied it was the ledger's own `MIN_MARGIN`. FA-4's pre-gate `proposal_shape` branch now denies the degenerate proposal in BOTH configurations, so the specific hole is closed, but the general one is not: an approval with no reservation is invisible to the next pass, which is the over-commitment §3's Phase B exists to prevent and the D3.47 residual restated with a second consumer. **Deliberately NOT fixed inside this ULTRAREVIEW**: making `ledger` required is a signature change that moves every construction site in the tree, which is a refactor and the freeze forbids it. Discharge = `ledger` required, the drill sites updated, and a `check_limiter_gate` arm requiring `reservation_id is not None` on every APPROVE | ARC 039+ | limiter |
| D3.352 | **I1 — *"nothing reaches broker-order without passing the Limiter"* — has NO instrument, and its ENTRY half has no subject: there is no `place_order` call site in `scripts/nixrisk/` at all** | ARC 038 (sub-agent A, FA-5), MEASURED by whole-tree AST enumeration | `grep -rn "Nothing reaches broker-order"` over the tree returns ONLY the frozen spec. `checks/check_order_path_bans.py` is the only gate scoped to "the order path" and the property it owns is a different sentence, stated in its own first line: *"the order path contains no retry machinery and no loop-blocking call."* The enumeration: 274 `.py` files under `scripts/` parsed with ZERO skipped for `SyntaxError` (so completeness is measured, not claimed), the mutating-verb roster DERIVED from `broker_seam.ORDER_PORT_VERBS` minus the read-only verbs — `place_order`, `cancel_order`, `flatten` — giving **84 call sites: 74 in `scripts/tests/`, 4 in `scripts/broker/`, 6 in `scripts/nixrisk/` (`coldstart.py:713`, `fills.py:443`, `flatten.py:499`, `flatten.py:556`, `flatten.py:614`, `survival.py:549`), and ZERO anywhere else** — including zero in `scripts/nixsentinel/`, which declares itself §14's one exception but does not call the order port. Indirect reaches: zero `getattr(x, "<verb>")`; three non-call bound-method loads, all in `scripts/broker/seam_simulate.py`'s own `expect_raises`; fifteen verb-name string literals, fourteen inside `scripts/broker/` and one benign step-name enum member at `scripts/nixrisk/recovery.py:506`. **So the flatten/cancel half of I1 is RESISTED and the entry half is CANNOT-MEASURE, not Pass** (`nix_check_contract.md` §17/rule 10): a gate is exactly what would be needed the moment an entry dispatcher is built, and an ULTRAREVIEW may not build one. The reach set is asserted NOW as a literal roster in `scripts/tests/test_arc038_a_gate_wall.py::test_the_ORDER_PORT_REACH_SET_is_exactly_the_LIMITER_and_the_ADAPTER`, which also asserts the entry half stays empty and reddens the day it does not. Discharge = a standing `checks/check_*` (or an arm on `check_order_path_bans`, extending the instrument that already derives this scope — doctrine C.9) requiring every `place_order` reach to sit behind a `GatePass` approval, once there is one to require | ARC 039+ | limiter |
| D3.353 | **Three of this arc's five repairs are gated by PYTEST ONLY, so `verify.py` on a box that never runs pytest cannot see them** | ARC 038 (sub-agent A), stated by its author at the moment of writing rather than found later | FA-2 (the blackout onset cancels at the venue), FA-3 (the sweep completes past a refusal; a partial sweep still books its §12.10:753 row) and FA-4 (a degenerate proposal is denied by the gate, not by the ledger) are measured by `scripts/tests/test_arc038_a_gate_wall.py` and by nothing in `checks/`. Only FA-1 got a standing gate arm (`check_limiter_gate.arm_partition_is_a_partition`). **The choice was doctrine C.9, not convenience, and is recorded so it can be revisited rather than rediscovered:** proving *the venue order is cancelled on onset* needs a broker double, a reservation ledger, the flatten executor and a real calendar window composed in ONE rig, which is `check_flatten`'s and `check_halt`'s composition and not `check_blackout_windows`' read-only window scope — extending that gate would have grown a window instrument into an execution instrument, and adding a fourth gate over the same subjects is the duplicate instrument C.9 forbids. This is the D3.10 / D3.190 asymmetry, third instance. Discharge = either an arm added to `check_halt` (multi-entry sweep with a REFUSING broker double — its `_onset_defects` drives exactly one entry against a `_Broker` that cannot refuse, which is why it was green over FA-3) and an arm added to `check_flatten` (the onset sweep driven against a broker that refuses one of three), or the ruling that a pytest suite in the runtime gate is sufficient coverage for a composed property | ARC 039+ | limiter |
| D3.354 | **An onset sweep that reaches an ALREADY-FILLED entry releases its reservation under the ONSET cause, so `committed` drops by the margin of a REAL position and §9's row names the wrong terminal path** | ARC 038 (sub-agent A, FA-6), MEASURED — the fill-vs-onset-cancel race driven in BOTH orderings against `broker_seam.StubBrokerOrder`'s real working book and real terminal states | `flatten.py:614-621` cancels and then `ledger.resolve(coid, cause, …)` unconditionally, reading nothing about whether the order is still working. ORDERING 1 (cancel first, fill after) is clean: the fill is refused, `Σ reservations` goes to 0.0, one release under `HALT_ONSET`, no exposure. ORDERING 2 (fill first, cancel after) leaves a REAL `('ES', 2)` position while `Σ reservations` has gone to `0.0` and the Plane-1 release row says `HALT_ONSET` — but §3:150-152's taxonomy is explicit that a FILL release *"converts to open-margin"*, which is a different fact. So `committed` under-counts by the position's full margin (the §15:987 C1 double-spend shape, entered from the fill side) and the fill handler that would later release under `FILL` meets the ledger's `DoubleRelease` refusal instead. §14's *exactly one terminal release* still HOLDS — the defect is the CAUSE and the accounting, not a leak, and that distinction is why no existing gate saw it. The sweep's only defence is its caller's book (`PendingEntriesPort.pending_entries` = *"Every pending ENTRY order"*, so a filled order is out of contract) and **there is no production implementation of that book** (D3.349), so the precondition cannot be checked by anything. **NOT fixed inside the freeze, deliberately:** the two available repairs are (a) read `query_order_status` before cancelling, which needs a verb `BrokerFlattenPort` withholds ON PURPOSE so the zero-wire claim stays legible (`flatten.py:205-215`) — trading a §14 property for a §9 one — or (b) make the fill handler win the book race, which is the Limiter event loop D3.349 says does not exist. The half that HOLDS is asserted as a standing control (`test_arc038_a_gate_wall.py::test_the_ONSET_CANCEL_and_a_FILL_are_SAFE_in_BOTH_ORDERINGS`), with the mis-booked cause named in its docstring. Discharge = the fill/cancel arbitration on the Limiter loop, booking `FILL` and converting to open margin when the venue says the order is terminal-filled | ARC 039+ | limiter |
