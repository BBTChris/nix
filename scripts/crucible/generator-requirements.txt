# Crucible calendar GENERATOR dependency pin — BUILD/dev-only.
#
# Deliberately NOT in checks/pinned_deps.json: that file is the single
# source of truth for the shared runtime .venv (install.sh installs it there
# and check_python_deps.py enforces conformance against the live venv
# forever, repairing drift back to pin). pandas_market_calendars must NOT be
# governed by that drift-repair loop -- it is a generator-only concern.
#
# ARC CRUCIBLE-DEPSPLIT: this file installs into `.venv-dev` ONLY, a SEPARATE
# venv from the runtime `.venv` -- never the shared one. CALENDAR-INFRA's own
# original text here said to install this into the shared `.venv`; doing so
# is exactly what produced D3.111 (docs/CHECK-DEBT.md): this library's own
# transitive tree (exchange-calendars -> tzdata, unconstrained) silently
# bumped tzdata outside ib_async's declared `<2026.0` range in that one
# shared venv. `.venv-dev` holds nothing else -- no ib_async, no pytest -- so
# its own tzdata resolution can never collide with the runtime venv's.
# Success #1's proof (runtime venv rebuilt from checks/pinned_deps.json +
# checks/requirements-runtime.txt alone carries neither this package nor
# exchange_calendars) plus scripts/tests/test_crucible_calendar.py's own
# isolated-subprocess test (spawns a bare interpreter with
# pandas_market_calendars/pandas/exchange_calendars excluded from sys.path)
# together prove calendar.py (the runtime query module) never needs this
# library present.
#
# Build and use `.venv-dev` (uv, not ad-hoc pip -- Success #7):
#   uv venv .venv-dev
#   uv pip install --python .venv-dev/bin/python -r \
#       scripts/crucible/generator-requirements.txt
#   .venv-dev/bin/python -m scripts.crucible.calendar_gen
# calendar_gen.py itself refuses to run under any other interpreter (its own
# venv-identity guard, `_REQUIRED_VENV_MARKER` below this module's imports) --
# a runtime-`.venv` invocation fails loudly (ImportError, no
# pandas_market_calendars there) and a wrong-*other*-venv invocation fails
# loudly and explicitly instead of silently half-working.
#
# Pinned for determinism (Success #2: same pinned inputs -> byte-identical
# output). Version recorded in the provenance stamp on every generation run.
pandas_market_calendars==5.4.0
