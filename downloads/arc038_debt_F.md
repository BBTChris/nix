# ARC 038 sub-agent F — CHECK-DEBT rows, ready to paste

Reserved block D3.398 – D3.407. All ten used. `owning module` tokens are taken from
`docs/CHECK-DEBT.md` "The controlled vocabulary" as it stands on disk — `limiter`,
`allocator`, `verify`, `scoring`. (The brief suggested `risk`; **there is no `risk`
token in the vocabulary**, and an unknown token is a loud `ProbeError`. Directive 5.)

| D3.398 | **§14's GO-timeout has NO implementation anywhere in shipped code, and a REAL `SIGKILL` of the GO holder wedges the one-in-flight lock permanently** | ARC 038 (sub-agent F, I5), MEASURED | §14:965 locks *"One in-flight action per strategy — and it can never wedge (GO-timeout)"*; C6 at §15:994 and §4:210-212 name the breaker and its example failure (*"e.g. Allocator died holding it"*). DRIVEN: a real child process published a GO over a real `ipc://` socket, the Limiter side took the lock through the shipped `StrategyRegistry`, the child was `SIGKILL`ed (reaped `-9`, `/proc/<pid>` gone), and **11.0 s later — past `limiter.go_timeout_s = 10` — the shipped `GatePass` still answered `deny \| in_flight_lock \| s1: one-in-flight lock held by c-1 (§4:210)` and the registration was still held.** An AST census over `git ls-files scripts` minus `scripts/tests/` found exactly ONE shipped site that clears an `in_flight` field (`recovery.py:465`, inside `force_deregister`, which destroys the whole registration) and NO shipped site that measures elapsed time against `go_timeout_s` — every other occurrence of the token is a Plane-1 event-type NAME (`plane1_sink.py`, `plane1_seed.py`, `projection.py`, `seam.py`, `nixscore/ema.py`) or the boot cross-validator in `risk_config.py`. So there is no *normal-resolution* release verb either: once the lock is taken the only exit is a force-deregistration, which requires the strategy to be presumed dead. Discharge = build the §4:212 timer (a `GO_TIMEOUT_T` deadline per outstanding GO, a `go_timeout` Plane-1 row, and a release-to-flat-and-free that is NOT a deregistration), plus a can-fail control that drives it. ARC 038 was forbidden to build, so this is the arc's headline BLOCKING finding. `scripts/tests/test_arc038_f_inflight_lock.py` pins the census and the wedge as a RATCHET so the fix cannot land unnoticed. | ARC 039 | limiter |
| D3.399 | **`StrategyRegistry` has no memory of a retired `client_order_id`, so a replayed GO's cid is re-admitted to the lock after a re-registration** | ARC 038 (sub-agent F, I5), MEASURED | The residual left by the FF2 guard landed in this arc. `ReservationLedger` keeps `_order_terminal` and refuses a cid that already reached a terminal path (*"a client_order_id is minted once and never reused"*). `StrategyRegistry` keeps no such set: DRIVEN — `take_in_flight('s3','c-late')` → `force_deregister('s3')` → `register('s3')` → `take_in_flight('s3','c-late')` was **ACCEPTED**, and the lock reports `held by c-late` for an action that already reached its terminal path. The new second-take guard closes the concurrent/duplicate case but not the replay-after-reset case, because closing it needs a new per-registry retired-cid store and that is not a minimal local change to a frozen file. Discharge = a retired-cid set on the registry (or a registry generation stamped into the cid) plus a control that replays a retired cid across a re-registration. | ARC 039 | limiter |
| D3.400 | **`check_plane1_hot_path` measures a `GatePass` built with `ledger=None`, so the ONLY I/O the shipped approve path performs is outside every timed region the gate reports** | ARC 038 (sub-agent F, I9), MEASURED | `scripts/plane1_hotpath_drill.py::_gate` returns `GatePass(clear, list(rules))` — printed from the object: `_ledger is None`. `GatePass._settle` therefore never takes a §3 reservation, so `ReservationLedger._book` → `_emit` → `Plane1Wal.enqueue` never runs inside the timed loop. Measured difference on the same box, same order, same picture, same manifest, n=4000: **ledgerless p50 8.2 / p99 9.8 / max 13.3 µs; with the real `ReservationLedger` over a real `Plane1Wal`, p50 34.3 / p99 38.4 / max 1169.8 µs** — an 88x tail the standing gate cannot see. Under `strace -c -e trace=write,fsync`: **4,202 `write(2)` calls for 4,200 approvals — one unbuffered write syscall per approved evaluation**, because `Plane1Wal` opens `"ab", buffering=0` deliberately (its own docstring: *"so `enqueue` issues the `write(2)` itself"*). The gate is NOT vacuous (2,543 `scripts/nixrisk/gate.py` call events traced during `baseline(50)`, and it reddened on both an implanted 2 ms block and an implanted per-evaluation `write(2)`), so this is a scope gap, not a dead instrument. Partly discharged here by `scripts/tests/test_arc038_f_hot_path_ledger.py`, which owns the append COUNT and the audit-event census over the real ledger; the standing GATE still reports on the ledgerless shape. Discharge = a fourth drill arm carrying the real ledger, with the append count asserted so a network sink behind it moves a number. | ARC 039 | verify |
| D3.401 | **`InFlightLockRule` is the ONE rule in `default_manifest` whose DENY branch no test and no gate ever drove — deleting it left three gates and 116 tests green** | ARC 038 (sub-agent F, I5), MEASURED | `grep -rn InFlightLockRule` over the whole tree returned exactly TWO hits: its own `class` statement (`gate.py:315`) and the one line of `default_manifest` that constructs it (`gate.py:944`). Nothing constructed it directly and every instrument handed `default_manifest` an always-clear `in_flight` port — even though the shared `Flag` double in `test_limiter_gate.py` already implements `in_flight(strategy_id)` and could have been blocked with one argument. PLANTED: the whole blocking branch removed (`if locked: return _blocked(...)` → `del locked, reason`). `check_limiter_gate`, `check_orphan_recovery` and `check_allocator_sizing` all stayed **pass**, and `test_limiter_gate.py + test_presize_integration.py + test_recovery.py + test_reservations.py + test_allocator_sizing.py` reported **116 passed**. The other eight rules' blocking branches ARE driven (HALT, blackout, tradability, staleness, coherence, cap, survival, ceiling), so this is one rule and not a class. Discharged in this arc by `scripts/tests/test_arc038_f_inflight_lock.py`, which drives the deny through the shipped manifest with the REAL `StrategyRegistry` and proves itself able to fail against a never-blocking replacement rule. Row kept because the CLASS — a manifest rule reachable only through a port shape no double is ever blocked in — has no standing detector. | ARC 039 | verify |
| D3.402 | **The §14 hot path takes FIVE separate wall-clock samples per gate evaluation, so one pass judges its four feeds and its clock-skew arm against five different instants** | ARC 038 (sub-agent F, I9), MEASURED | With the real `StalenessFlagPort` over a real `FreshnessTracker` and the real `ClockSkewMonitor` wired into `default_manifest`, a counting clock recorded exactly **5.0 clock calls per evaluation over 2,000 and 4,000 evaluations** — one `FreshnessTracker.reading` per configured feed (`balance`, `calendar`, `margin`, `price`) plus `ClockSkewMonitor.read`. §14 fixes the hot path at *cache reads + arithmetic only* and §11.1 at *"a dict lookup, never a scan"*; a `datetime.now(UTC)` per feed is neither, it scales with the configured feed set, and it means a single "atomic" pass can straddle a clock boundary and age two feeds against two different `now`s. Not blocking I/O (zero audit events were observed), so it is not the §11 item 6 failure — it is an atomicity and cost observation. Discharge = one `now` sampled once per pass and threaded through the phase-A ports (the ports already take injectable clocks), plus a control that counts the samples. | ARC 039 | limiter |
| D3.403 | **D3.346's load sensitivity is the CPython GIL handoff cadence, reproduced deterministically, and the concurrent arm now pins it — the residual is the quadratic `drain_once`** | ARC 038 (sub-agent F, I9), MEASURED | D3.346 recorded `check_plane1_hot_path.MIN_OVERLAP_COMMITS = 3` firing inside a loaded census and passing alone 40 s later. Under the six-sibling load of ARC 038 Stage 1 the floor did NOT fire (8 consecutive runs, overlap 6 every time), so the load alone does not reproduce it. The MECHANISM does: `sys.setswitchinterval(0.05)` — the drill's own docstring names the switch interval as the convoy's cause — put **3 of 6 runs below the check's floor of 3 (overlaps 1, 2, 2)**, i.e. CANNOT_MEASURE; at 0.001 (the value chosen), **6 of 6 runs reached overlap 6** and the arm's max fell from ~5,200 µs to 1,161–1,421 µs. `concurrent()` now pins the switch interval for the arm's duration, restores it in a `finally`, and reports it together with `iterations_driven` and `overlap_stretch_cap_hit`. The value was chosen BY MEASUREMENT and the rejected candidates are recorded in the constant's own comment, because "as small as possible" is wrong: at 0.0005 the overlap was 6/6 but the p99 reached **102.2 µs against the gate's 10x-BASELINE bound of ~100 µs** — trading a CANNOT_MEASURE flake for a FAIL flake. At **0.001** the overlap was 6/6 on eight runs, p99 16.3–20.4 µs against the unpinned arm's 15.3–17.5 µs (a ~3 µs cost), and the arm's MAX fell from ~5,200 µs to 1,161–1,421 µs. The bias is AGAINST a pass, never toward one: the hot loop yields more often, so its own p99 rises. Residual, NOT fixed here: `GroupCommitWriter.drain_once` re-parses the entire WAL per drain (ARC 035's own reported quadratic), which is why the arm needs `MAX_WAL_ROWS` at all. | ARC 038 | verify |
| D3.404 | **`check_uncalled_entry_points` reports `HeartbeatMonitor.beat` as UNCALLED while a gate calls it, because the receiver comes back from a factory the resolver cannot type** | ARC 038 (sub-agent F, I5), MEASURED | The gate's live verdict lists `scripts/nixrisk/recovery.py::HeartbeatMonitor.beat` under UNCALLED — *"no call site in shipped code and is not in a gate either"*. `checks/check_orphan_recovery.py:454` calls `reset.beat("s", now=1.5)`, where `reset = loaded.recovery.heartbeat_from_config(...)`; a `settrace` census over a real run of that gate recorded `beat` x1, `miss` x4, `poll` x8, `arm` x14, `force_deregister` x8, `recover` x7. The correct bucket is `gate_only`, which is a finding of a different and milder kind. The gate's own stated limit 2 promises an unresolved receiver *"suppresses the finding rather than creating one"*; a factory return type is a third case that does neither. Consequence: the row is real (there is still no SHIPPED caller) but its bucket is wrong, and a reader concluding "not even a gate drives this" would be misled. Discharge = resolve a call whose receiver is the return of a module-level factory in the same file set, or move the row to `gate_only` by hand and record why. | ARC 039 | verify |
| D3.405 | **Nothing in the tree watches the ALLOCATOR's liveness, which is the one party §4:212 names as the GO-timeout's example failure** | ARC 038 (sub-agent F, I5), MEASURED | Three liveness observers exist and none of them looks at the Allocator: `scripts/nixsentinel/watchdog.py` watches the **Risk Engine** heartbeat (§12.1); `scripts/nixscore/liveness.py::PublisherLiveness` watches the **Scoring publisher**'s socket; `scripts/nixrisk/recovery.py::HeartbeatMonitor` watches **strategies** (and is unwired, D3.407). §4:212's breaker is on the **strategy** side of a lost GO — *"if a strategy receives no sized/denied feedback within T"* — so even a fully wired `HeartbeatMonitor` could not break the wedge D3.398 reproduces, because in that scenario the strategy is ALIVE and the Allocator is the corpse. Discharge = either a liveness observation of the Allocator on the same footing as the Sentinel's of the Limiter, or the §4:212 strategy-side timer, and a drill that kills the Allocator mid-GO and bounds the time to flat-and-free. | ARC 039 | allocator |
| D3.406 | **`check_plane1_hot_path` reports a CONSTANT `site` naming a collaborator it describes as "off the hot path", so a defect planted ON the hot path reddened while the report pointed elsewhere** | ARC 038 (sub-agent F, I9), MEASURED | `_SITE = "scripts/nixrisk/wal.py:GroupCommitWriter.drain_once (off the hot path)"` is returned as the `site` of every FAIL and every CANNOT_MEASURE. With a 2 ms `time.sleep` planted inside `scripts/nixrisk/gate.py:GatePass.evaluate`, the gate went RED with an accurate DETAIL (`§11 item 6 … p99 7324.9us, above 200us`) and a site that is neither the planted defect nor on the path the detail is about. Check-contract rule 11 makes the REASON the assertable artifact and the detail carries it, so the gate is not vacuous — but a constant site carries no information about which subject moved, and this one actively misdirects. Fixed here by pointing `_SITE` at the measured hot path with the commit path named beside it; kept as a row because the general shape (a `site` that is a module constant rather than a function of the failing arm) is present in other timing gates and has no detector. | ARC 038 | verify |
| D3.407 | **The wedge-breaker that DOES exist is on no live path: `HeartbeatMonitor`, `StrategyRegistry` and `RecoverySequencer` have no shipped caller at all** | ARC 038 (sub-agent F, I5), MEASURED | `grep` over the shipped population (`git ls-files scripts` minus `scripts/tests/`) finds no caller of `HeartbeatMonitor.beat/miss/poll/arm`, `StrategyRegistry.register/take_in_flight/force_deregister` or `RecoverySequencer.recover` outside `checks/` and `scripts/tests/`; `recovery.py` is not even in `checks/uncalled_entry_points_baseline.json`, which is why that gate is currently RED on it. So §4:260-274's whole orphan-recovery spine — the only mechanism in the tree that can release a held in-flight lock — is built, gated, proven correct when driven, and driven by nothing. This is the ARC 033 / D3.150 / D3.178 class at the level of a safety spine rather than a stop distance. It is stated separately from D3.398 because the two discharges are different work: D3.398 is a mechanism that does not exist, this is a mechanism that exists and has no §5 event loop to call it. Discharge = the Limiter loop (or its §12.2 supervised process) constructing the registry and the monitor at boot and polling them, with a control that observes the poll happening rather than the monitor answering correctly. | ARC 039 | limiter |
