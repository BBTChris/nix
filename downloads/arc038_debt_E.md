# ARC 038 sub-agent E — CHECK-DEBT rows, ready to paste

Reserved id block: **D3.388 – D3.397**. Used: D3.388–D3.395. Unused: D3.396, D3.397.
Format is the ledger's 6 columns. Owning-module tokens are taken from the ledger's own
**controlled vocabulary** (`limiter`, `database`, `verify` — note `risk` is NOT a token).
Do NOT paste rows for FE4, FE7, FE8 or FE9: they were DISCHARGED in this arc.

| D3.388 | **Plane-1 sole-writership and append-only are enforced on a ROLE nothing runs as — the runtime identity is the log's OWNER and a SUPERUSER, and it can INSERT, UPDATE, DELETE and TRUNCATE the money record** | ARC 038 (sub-agent E, I8), MEASURED against the LIVE `nix_plane1` | `databases/schema/plane1.sql:33-43` argues *"APPEND-ONLY BY PRIVILEGE, NOT BY TRIGGER … the writer role is GRANTed SELECT and INSERT … and is granted nothing else, ever"*. Measured: `current_user=bbt`, `rolsuper=t`, and `relacl` on the log is `{bbt=arwdDxtm/bbt, nix_limiter=ar/bbt, nix_reader=r/bbt}`. `scripts/nixrisk/plane1_sink.py:315-343` passes no `-U`, so every process connects as the OS user, which `pg_hba` (`local all all peer`) resolves to `bbt`. Driven as the ordinary runtime user with no role assumed and no WAL, each inside `BEGIN … ROLLBACK` so the record was not mutated: INSERT returned `967\|filled\|ARC038E-IMPOSTOR`, UPDATE returned `1\|REWRITTEN BY ARC038E`, DELETE returned `1`, TRUNCATE reported `rows now 0`. **What stopped each attempt: NOTHING BUT CONVENTION.** `check_plane1_schema` ARM 4/9 and `check_plane1_sole_writer` ARM A both probe `nix_limiter`/`nix_reader` ONLY — measured: zero `current_user`/`session_user`/`usesuper`/`rolsuper` probes of the session in either gate — so the gate evidence *"append-only proven BY PRIVILEGE … and BY ATTEMPT"* is true of a role nothing runs as. No SQL closes this: a superuser is exempt from every grant and an owner holds UPDATE/DELETE/TRUNCATE by ownership. Discharge = a NON-SUPERUSER login identity for the runtime (a `pg_ident` map or a TCP/scram credential, since `pg_hba` is peer-only), the sink connecting as it, and a gate arm that measures the EFFECTIVE connecting identity's privileges rather than a role's. `ALTER ROLE bbt NOSUPERUSER` is not a candidate — it would break every other gate on the box. Standing control landed this arc: `scripts/tests/test_arc038_e_plane1_record.py::test_the_APPEND_ONLY_refusal_is_caused_by_the_ROLE_and_by_NOTHING_ELSE`, both halves | ARC 039 | database |
| D3.389 | **The shipped Plane-1 sink is a public class any module can construct and `.commit()` — five non-Limiter processes each landed a durable row with `Plane1Wal` never constructed** | ARC 038 (sub-agent E, I8), MEASURED in five real subprocesses against a scratch database built from the SHIPPED DDL | §9:549's ordering *"Enqueue → durable local WAL → shared-pool writer → group-commit"* is the invariant that makes the crash gap one-sided (`scripts/nixrisk/wal.py:485-501`), and `scripts/nixrisk/plane1_sink.py:457`'s `commit(self, rows)` demands no evidence of WAL durability. Driven from Scoring (`nixscore.ema`), the Allocator (`nixalloc.caps`), the Sentinel (`nixsentinel.marker`), the ARC 037 realized-P&L module and a check's own import world — each child launched with an explicit `env=` that FILTERS `/home/bbt/nix/scripts` out of `PYTHONPATH` and prepending this worktree's (D3.344), each printing the `__file__` it imported from. All five landed 1 row: `IMPOSTOR-scoring\|signal\|0`, `IMPOSTOR-allocator\|signal\|1`, `IMPOSTOR-sentinel\|signal\|2`, `IMPOSTOR-realized\|signal\|3`, `IMPOSTOR-check\|halt_set\|4`. Note the `wal_seq` values 0–4: each fresh sink minted a plausible sequence number off `max(wal_seq)+1` for a row that never touched a WAL, so **`wal_seq` is not evidence of WAL passage**. `check_plane1_sole_writer` ARM B1/B2/B3 detect composing an INSERT literal, DEFINING a new sink, and constructing an unrouted `EventRow` — CALLING an enumerated sink is none of the three, and the gate's own docstring concedes the blindness while pointing at ARM A, which D3.388 shows to be hollow on this cluster. Both halves are open at once. Discharge = the process/core boundary §2:35 and §10 actually draw, plus D3.388's database identity; a caller-frame assertion inside `commit()` would be fragile theatre | ARC 039 | limiter |
| D3.390 | **`wal_seq` is neither UNIQUE, FAITHFUL nor GAPLESS, and nothing in the tree detects any of the three — a healed dedup and a lost row have the same signature** | ARC 038 (sub-agent E, I8), MEASURED with two racing subprocesses, a §12.4-shaped replay, and a real SIGKILL | `databases/schema/plane1.sql:151-160` defines the column as *"the local WAL record number this row came from. The WAL is the only place ordering is authoritative"*, and its only constraint is `CHECK (wal_seq >= 0)` (`:168`) — no UNIQUE anywhere. `scripts/nixrisk/plane1_sink.py:423-427` is an unsynchronised read-modify-write over `max(wal_seq)`, and `:498` advances `_next_seq` over rows `ON CONFLICT` discarded. **Uniqueness:** two sinks both resolved `next_wal_seq() -> 0` and both landed 3 rows — 6 rows, 3 distinct `wal_seq`, `wal_seq=0` shared by `RACER-A-0` and `RACER-B-0`. **Gaplessness:** a dedup replay reported `landed=0 deduplicated=3` and burned 3,4,5, leaving `0,0,1,1,2,2,6` with `MISSING wal_seq: 3,4,5`. **Faithfulness:** after a SIGKILL at 4 of 12 banked and a restart heal, twelve WAL records became twelve rows with ZERO loss and `wal_seq=[0,1,2,3,8,9,10,11,12,13,14,15]` — WAL record #4 landed carrying `wal_seq` 8 and 4–7 do not exist. So a hole is not evidence of loss and a genuine loss looks identical. `check_plane1_crash_gap`'s `SUBJECTS` is `("scripts/plane1_crash_drill.py",)` and its four arms consume the drill's JSON; it never reads `wal_seq` continuity in any record, and no other check mentions it. There is no vacuous detector — **there is no detector**. Discharge = the sink being TOLD the row's WAL index (`GroupCommitWriter._cursor` knows it; `CommitSinkPort.commit(rows)` cannot carry it, and widening that Protocol breaks `RecordingSink` and every double), plus a gate that reads the record's own `wal_seq` continuity. A design question, not a patch | ARC 039 | limiter |
| D3.391 | **§9's first three event types — `signal`, `accepted`, `denied` — are emitted by NO production path, so the money record holds no row for any gate decision, and the coverage gate reports it and passes** | ARC 038 (sub-agent E, I8), MEASURED by enumerating `EventKind`, resolving every member, and grepping the tree for emitters | §9:549 requires *"one row per transition (signal, accepted, filled, … denied …)"* and §3:111's *"deny (rule named, fail-fast)"* exists so the deciding rule is ON the record. The mapping is TOTAL (18/18 resolve or raise `UnmappableEvent`), the schema carries all three enum values, and `scripts/nixrisk/gate.py` — the two-phase gate — contains NO reference to `Plane1Port`, `EventRow` or `enqueue`. Every `EventKind.SIGNAL`/`ACCEPTED`/`DENIED` occurrence outside `seam.py` and `plane1_sink.py` is a test, a drill, or `check_plane1_sole_writer`'s own probe rows. `check_plane1_event_coverage` reports it honestly on every run — `accepted=TRANSPORT-ONLY; denied=TRANSPORT-ONLY; signal=TRANSPORT-ONLY` — and PASSES, because its ratchet is deliberately asymmetric (*"a type gaining a producer is reported and does NOT fail"*) and a type that never had a producer has nothing to lose. `plane1_sink.UNROUTABLE_PLANE1_EVENTS` censuses the SCHEMA-side gap with a reason each; the mirror-image gap — a member with a mapping, an enum value and no emitter — is counted nowhere and had no ledger row until this one. Discharge = the emitter in the gate's settle path (`GatePass._settle`), plus turning the coverage gate's TRANSPORT-ONLY count into a ratcheted figure so it cannot grow silently | ARC 039 | limiter |
| D3.392 | **The Limiter's margin cap is fed by NO stop distance, and its cap FRACTION is fed by no config either — `agg_margin_cap_pct`'s only reader in the tree is its own boot validator** | ARC 038 (sub-agent E, I12), MEASURED by driving the shipped rules and by exhaustive grep | §7:481 (*"`risk_$` is honest only if sized against stop + expected slippage"*) and §7:501/§7:511 price bucket exposure from the distance. Driven: holding the proposal constant and swinging `stop_ticks` 1 → 20 → 1 000 000 leaves `AggregateMarginCapRule` at `SIZE_DOWN sized=69` all three times — byte-identical. Confirmed structurally: `stop_distance\|stop_ticks\|initial_distance_ticks\|StopState\|StopBook` has ZERO hits in `scripts/nixrisk/gate.py`; the two cap rules' entire input set is `order.margin_per_contract`, `order.proposed_margin`, `picture.balance`, `picture.committed`, `picture.deployable`. Separately, `agg_margin_cap_pct` in `risks/limiter.config.json` is read by exactly one place in the tree — `scripts/risk_config.py:254`, its own validator — while every fraction reaching `AggregateMarginCapRule` is a drill/check/test literal (`plane1_hotpath_drill.py:231`, `plane1_degraded_drill.py:827`, `check_limiter_gate.py:813`). So `interlock.margin_cap_within_deployable` validates a relationship between two numbers no shipped rule instance reads. `check_allocator_caps` is genuinely strong (7 arms, spec-parsed buckets, a `max()`-shaped falsifier) but its `SUBJECTS` is `nixalloc/caps.py`, `nixalloc/contention.py`, `risks/allocator_caps.config.json` — `gate.py` is not in scope, so both facts were invisible to it by construction. Discharge = a manifest loader that reads §12A's knobs out of `risks/limiter.config.json` into `default_manifest`, and an architect ruling on whether §7:501's distance-priced exposure belongs in the Limiter's cap or stays the Allocator's | ARC 039 | limiter |
| D3.393 | **D3.150/D3.178 one level up: `StopBook`, `FillHandler`, `PositionOriginWriter` are constructed by NOTHING in production, and `NetLiqMarkPort` has no production implementation at all — every net-liq mark in the tree is a hardcoded `10_000_000.0, True`** | ARC 038 (sub-agent E, I12), MEASURED by grep over `scripts/` and `checks/` minus `scripts/tests/` | ARC 033 recorded D3.178 as *"`StopBook.arm` and `PositionOriginWriter.on_fill` both have ZERO production callers"*. They now HAVE callers — `scripts/nixrisk/fills.py:626` and `:573`, inside `FillHandler` — and `FillHandler(`/`LimiterFillSink(` are constructed only in `checks/check_fill_handler.py:633,639`; `PositionOriginWriter(` only in `check_fill_seam.py:742`, `check_fill_handler.py:619`, `check_origin_write.py:446`; `StopBook(` only in `plane1_degraded_drill.py:865,1111`, three checks and `scripts/tests/`. So the mechanism is CALLED by a mechanism nothing constructs — the exact trap D3.150 records (*"a decision recorded is not a mechanism landed, and a mechanism landed is not a mechanism CALLED"*), displaced one frame up rather than closed. Independently, `def mark(self) -> tuple[float, bool]` exists only in the Protocol declaration (`gate.py:184`), two drills (`plane1_hotpath_drill.py:217`, `plane1_degraded_drill.py:771`, both returning `10_000_000.0, True`), tests and checks — and `survival.py:379`'s `SurvivalWatch.mark`, the thing that actually COMPUTES a net-liq mark, has zero production callers and is not a `NetLiqMarkPort`. So §6.5's survival floor and §14's *"survival is watched on net-liq"* are, in this tree, evaluated against a literal. Discharge = the Limiter's broker-event handler constructing the fill chain, plus wiring `SurvivalWatch` to `NetLiqMarkPort` | ARC 039 | limiter |
| D3.394 | **The per-tick stop path has no finiteness guard: a `NaN` tick makes a BREACHED stop report NOT breached, and one `+inf` tick corrupts `level` and `high_water` to `inf` permanently** | ARC 038 (sub-agent E, I12), MEASURED against a real `StopBook` | §14:965 *"Every uncertainty resolves toward flat"*. `scripts/nixrisk/stops.py:255` guards `arm` (twelve poisoned distances and fill prices, twelve refusals — proof of resistance), but `maintain` (`:292`), `breached` (`:313`), `_breached` (`:187`) and `_ratchet` (`:373`) apply no guard to `price`. Driven on a LONG armed at 4500.00 with 20 ticks at 0.25 (level 4495.0): `breached(4494.00) -> ['cX']`, `breached(4510.00) -> []` (both correct), then `breached(NaN) -> DOES NOT FIRE` and `breached(+inf) -> DOES NOT FIRE` — `NaN` fails every comparison, so a stop that cannot answer reports "not breached", an uncertainty resolving AWAY from flat. On a TRAILING stop, `maintain(NaN)` is a silent no-op and `maintain(+inf)` sets `level=inf, high_water=inf`, after which every price satisfies `price <= inf` and the stop reports breached forever. `check_synthetic_stop_only`'s three arms are broker imports, order verbs and native stop order-type literals — a `stops.py` whose `_valid_distance` always returned `0` would still PASS it — so this is outside its declared scope, not a defect in it. NOT fixed in this arc deliberately: a per-tick `math.isfinite` is a §11:579 hot-path cost against *"Hot path = cache reads + arithmetic only"*, and whether the guard belongs on the tick or at the price ring's producer is an architect's call. Discharge = that ruling, then the guard, with a both-halves control driving a NaN and an inf tick | ARC 039 | limiter |
| D3.395 | **`risk_config._read_one` parses config with plain `json.loads`, which ACCEPTS the bare `NaN` / `Infinity` tokens — a non-finite knob can physically enter the config set and is now caught only by value, one layer later** | ARC 038 (sub-agent E, I12), MEASURED: `json.loads('{"netliq_safety_pad": NaN}')` returns `{'netliq_safety_pad': nan}` | The value-side hole was DISCHARGED this arc — `_positive_scalars` now refuses a non-finite scalar (it previously used `leaf <= 0`, which `NaN` passes, and `_pct_range` only inspects keys ending `_pct`, so `netliq_safety_pad=nan`, `netliq_safety_pad=inf` and `ledger_drift_tolerance_usd=nan` were all ACCEPTED AT BOOT), and `SurvivalHeadroomRule.__init__` now refuses a non-finite pad. What remains is the FILE boundary: JSON's own grammar has no `NaN`, so a config carrying one is malformed by the standard and `json.loads` accepts it as a CPython extension. Discharge = `json.loads(..., parse_constant=<raise>)` in `_read_one`, so a non-finite literal is refused where it enters rather than wherever a validator happens to look. Cheap, and it turns a value check into a grammar check | ARC 039 | limiter |
