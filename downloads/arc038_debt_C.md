# ARC 038 sub-agent C — CHECK-DEBT rows, ready to paste

Reserved block **D3.368 – D3.377**; eight used, D3.376–D3.377 unused.
Format is the ledger's exact 6 columns. `owning module` token is **`limiter`** —
taken from `docs/CHECK-DEBT.md`'s own *"The controlled vocabulary"* section (line
362), which owns `scripts/nixrisk/`. (The brief suggested `risk`; that token does
not exist in the vocabulary and an unknown token is a loud `ProbeError`. Directive
5: verified on-disk state outranks the brief.)

| D3.368 | **`ProtectiveFlatten.unbooked` is recorded but ROUTED NOWHERE — a flatten §9 could not record raises no §12.9 alert** | ARC 038 (sub-agent C, FC1), MEASURED | ARC 038 discharged FC1 by making `flatten.py:_book` RECORD a Plane-1 append failure instead of propagating it: measured against a real disk-critical WAL (`RLIMIT_FSIZE`, kernel `EFBIG`, errno 27), a three-target protective flatten went from `flatten_calls=["MESU6"]` with `still_open_at_broker=["MNQU6","MYMU6"]` to all three closed and `still_open_at_broker=[]`, with `fire_raised=""`. §12.4:625 requires exactly that (*"open positions remain protected"*), and §14:968 makes flat the resolution. But the residual it creates is a state nobody is told about: the Limiter FLATTENED and §9 has no row, which is §12.4's degraded-persistence condition and a §12.9 push alert. `ProtectiveFlatten.unbooked` is the observable (kind, trade_id, strategy_id, symbol, ts, and the port's own reason text); NOTHING reads it. It is a public ATTRIBUTE and not an accessor for a measured reason: the first shape was an `unbooked_rows()` accessor, and `check_uncalled_entry_points` failed the commit naming *"scripts/nixrisk/flatten.py::ProtectiveFlatten.unbooked_rows … NEW uncalled surface"* — correctly, since nothing shipped can call it until this row is discharged, and its baseline ratchet may only shrink. `nixrisk/degraded.py` already has the adapter shape — `PersistenceAlerts` over `survival.AlertTier`/`survival.Alert` — so discharge is: bind `ProtectiveFlatten.unbooked` to a `PersistenceAlerts` tier at the one place a `ProtectiveFlatten` is constructed in production, and gate it. There is no such place yet (all eight construction sites are gates and tests), so this cannot be discharged before the Limiter has a daemon. Control that pins the current state: `scripts/tests/test_arc038_c_exit_brake.py::test_the_UNBOOKED_ROWS_are_RECORDED_and_NAME_the_persistence_reason`. | the arc that builds the Limiter daemon | limiter |
| D3.369 | **`reservations.py:_emit`'s stated residual is worse than stated: the raise ABORTS the caller's whole sweep** | ARC 038 (sub-agent C, FC1 second site), MEASURED | `reservations.py:498` writes its Plane-1 row after the store mutation and names its residual as *"an enqueue that raises leaves a settled reservation with no Plane-1 row"* (CHECK-DEBT D3.53). Measured, the consequence is larger: the raise leaves the CALLER's loop. Traceback taken from the drive, not read off the page — `flatten.py:cancel_entries_on_onset → reservations.py:368 resolve → :441 _settle → :498 _emit → degraded.py:378 enqueue → wal.py:307 enqueue` raising `DiskCritical` — and a three-entry blackout-onset sweep cancelled ONE: `cancel_calls=["c-1"]`, `uncancelled_entries=["c-2","c-3"]`, `reservations_still_outstanding=["c-2","c-3"]`. Two entries left WORKING at the venue inside a window they were not approved for, which is what §3:174 exists to prevent. ARC 038 closed the SWEEP from `flatten.py` (catching per entry, recording on `ProtectiveFlatten.unbooked`, continuing — post-fix: all three cancelled, all reservations released, `raised=""`), deliberately WITHOUT editing `reservations.py`: that file is another sub-agent's invariant this arc and cross-branch edits to one table/file are how ARC 037 got its defects. Owed: decide whether `_emit` should absorb its own failure (making every OTHER caller safe too, not just the onset sweep) and whether D3.53's row should be re-scoped. Discharge = the ruling plus a control that drives a second `resolve` caller under disk-critical. | ARC 039 | limiter |
| D3.370 | **`ProtectiveFlatten`'s arbiter now holds a lock; §5's single-thread rule is still unenforced everywhere else in the Limiter** | ARC 038 (sub-agent C, FC2), MEASURED | `request_close` read `self._closed` (`flatten.py:534`) and wrote it six lines later (`:567`) with nothing between them. Driven with two real `threading.Thread`s and the read window forced open by a `dict` subclass (not a sleep), the recorded winner became `DISCRETIONARY` with `hard_reset=False` — §3:172's LOCKED *"protective exit always wins"* lost, and §4's one-in-flight slot left wedged. Interleaving observed: `['held:read prior=None','free:read prior=None','free:wrote protective','held:wrote discretionary']`. ARC 038 discharged it with a blocking `threading.Lock` (blocking, not `acquire(False)`: refusing would let a discretionary close in flight REFUSE a protective one). Post-fix, protective wins in BOTH arrival orders with `hard_reset=True`. The residual: `picture.py:350` REFUSES a second writer, `flatten.py` now SERIALISES one, and the rest of `scripts/nixrisk/` does neither — `ExecutionLedger`, `ApprovedOrderBook`, `EntryOrderOrigins`, `StopBook` and `FillHandler._armed` are all bare-dict read-modify-write under the same unenforced §5 convention. Owed: one ruling on how §5 is enforced (an owner-thread latch at the loop boundary is the obvious candidate, and is cheaper than N locks), then apply it once. Controls: `test_arc038_c_exit_brake.py::test_PROTECTIVE_WINS_under_a_REAL_THREADED_RACE_in_BOTH_orders` (both orders, both halves) and `::test_the_SEQUENTIAL_precedence_rules_are_UNCHANGED_by_the_lock`. | ARC 039 | limiter |
| D3.371 | **No gate drives a RE-DELIVERY of one execution through `LimiterFillSink`, which is the only layer that invents the report's `ts`** | ARC 038 (sub-agent C, FC3), MEASURED | §2A's `on_fill` carries no timestamp, so `fills.py:718` invented one per DELIVERY, and `ts` is in `execution._IDENTITY_FIELDS` (`execution.py:115`). An exact re-delivery therefore disagreed with itself on the one field the venue never sent, and `ExecutionLedger._on_duplicate_key` raised `ContradictoryExecution: … ts: 1787095703.778361 then 1787095703.7784965`. Replaying the identical report with the ORIGINAL stamp returned `DUPLICATE`, so the stamp was the whole defect. §15:1006 and §12.4's reconnect case both make a re-delivery EXPECTED. ARC 038 discharged it by memoising the stamp per `(client_order_id, exec_id)` — post-fix `ledger_duplicates=1`, `contradictions=0`, position unmoved. The GATE gap stands: `scripts/tests/test_execution.py` proves dedup at the LEDGER with hand-built reports that share a `ts` by construction, and `checks/check_fill_handler.py` drives the sink but only with `_LATE_FILL = ("CO-1","e2",1,4)` — a NEW `exec_id`, i.e. a later fill. Owed: a re-delivery arm inside `check_fill_handler`'s `_run_drive`, so the standing gate covers what the new pytest suite now covers. Control meanwhile: `test_arc038_c_open_is_confirmed_fill.py::test_a_RE_DELIVERED_FILL_is_a_DUPLICATE_not_a_CONTRADICTION`, with a `_PerDeliveryStamp` falsifier proven to reproduce the raise. | ARC 039 | limiter |
| D3.372 | **A fill the execution ledger INGESTED but the origin write REFUSED leaves §3's table and §12.7's mirror reading FLAT over a real position — and records nothing** | ARC 038 (sub-agent C, FC4), MEASURED — **BLOCKS, not banked by choice** | `positions.py:547` raises `UntradableSymbol` inside `_row`, which runs AFTER `positions.py:470`'s `self._ledger.ingest(report)` and after `FillHandler._arm` armed the stop and `IocRemainder` released the reservation. Driven end-to-end (real `ipc://` bus, real `PictureMirror`, real WAL) with `margin_per_contract` empty for the filled symbol — §4:198's not-tradable condition, reachable mid-session from a margin poll that drops a symbol: a real 2-lot fill produced `position_table_states=[]`, `mirror_open_trades=[]`, `sum_open_margin=0`, `wal_kinds=["reservation_taken","reservation_released"]` (no fill trace), `writer_unstopped_records=0`, while `exec_ledger_net_qty=2` and `reconcile` reported `drift=2, agrees=false`. §7:501 prices bucket exposure from the published table, so the held position is priced at ZERO and §7's correlation cap ADMITS MORE — D3.136's fail-open under a new spelling. The asymmetry is the defect: the SIBLING refusal (`positions.py:507 _refuse_unstopped`) deliberately records an `UnstoppedRecord` *"where a supervising loop can act on it instead of vanishing into a log"*; this path records only a counter. Also observed: `sum_reservations` stayed 2000.0 while `reservations_outstanding` was empty, so `committed` carries a phantom. NOT discharged, and deliberately not softened: the repair needs an architect ruling on WHICH surface carries the condition (publish the row anyway — with what margin figure? — or hand `nixrisk.flatten` an `UNCERTAINTY` trigger from this site) plus a consumer, and neither is a minimal local change to a frozen file. The state is PINNED by `test_arc038_c_open_is_confirmed_fill.py::test_a_REFUSED_ORIGIN_WRITE_leaves_the_PICTURE_and_the_MIRROR_reading_FLAT`, whose assertions name themselves as the ones to rewrite when it is fixed. | ARC 039 | limiter |
| D3.373 | **`check_plane1_degraded` is green over §12.4's *"open positions remain protected"*: its C2 arm proves DETECTION and never drives EXECUTION** | ARC 038 (sub-agent C, FC5), MEASURED | `scripts/plane1_degraded_drill.py:1160-1163` ends the disk-critical arm at `breached = book.breached(STOP_SYMBOL, breach_price)`. `breached()` is arithmetic over in-memory state and cannot fail for a disk reason, so asserting it under disk-critical asserts a tautology — the `CHECK-A7` shape one layer out: not a classifier whose output is a constant, but a SUBJECT whose outcome is independent of the deprivation applied. `ProtectiveFlatten` is never constructed in the drill and no broker seam exists in the child. Proven by plant: making `request_close` book its Plane-1 row BEFORE the broker call (so a disk-critical WAL means the flatten never happens AT ALL) left `check_plane1_degraded` rc=0, `check_flatten` rc=0, and `pytest test_flatten.py test_check_flatten.py test_degraded.py test_wal.py test_exit_integration.py test_session_flatten.py -q` at **93 passed**; restore was byte-identical (`sha256 3386a57f…`). The spec sentence is *"open positions remain **protected**"*, and a position is protected when it is FLATTENED. Owed: a C3 arm in `scripts/plane1_degraded_drill.py` that builds a `ProtectiveFlatten` in the same disk-critical child and requires the OBSERVED `broker.flatten` invocation for every target, plus the assertion in `check_plane1_degraded`. Not done from this branch: the drill is `check_plane1_degraded`'s subject and editing another invariant's gate from a parallel branch is the ARC 037 cross-branch defect. `scripts/tests/test_arc038_c_exit_brake.py` carries the measurement meanwhile (real child, real `RLIMIT_FSIZE`, three targets, `env=` explicit and the child's `nixrisk.flatten.__file__` asserted against the tree — D3.344 ruled out). | ARC 039 | limiter |
| D3.374 | **§9's durable record cannot say a position ever OPENED: `EventKind` has no `FILLED` member, so `projection.py`'s only position-creating handler can never fire from this tree** | ARC 038 (sub-agent C, RC5), MEASURED | `projection._HANDLERS` keys on `filled`/`cancel`/`closed`/`protective_exit`/`sentinel_flatten`, and `_on_fill` is the only handler that creates size. `[e.name for e in EventKind]` measured as the 18 members `SIGNAL … QUARANTINE` with **no `FILLED`**, so `plane1_sink.EVENT_KIND_TO_PLANE1` cannot map what does not exist and the Limiter cannot enqueue a `filled` row. Verified end-to-end: a real fill through the shipped entry seam left `wal_kinds=["reservation_taken","reservation_released"]`. Consequence for §14:970: the projection cannot manufacture an optimistic open (good), but only because it is never fed (not good) — it is exercised solely by hand-written rows, which is how this arm had to drive it (a throwaway `nixp1t_arc038c_*` database from `databases/schema/plane1.sql`, one hand-inserted `filled` row). Adjacent to and NOT the same as D3.281 (which records the absent fill FEED): this row is about the absent `EventKind` MEMBER and the projection handler it strands. Second, smaller finding in the same arm: a `partial` fold state maps to `PositionState.OPEN` in `projection.py:830`, so a consumer of `position_rows` cannot tell a resolved position from one whose remainder is still working — §4 makes position = actual filled quantity so the SIZE is right and this is not an optimistic open, but the fact is erased at the seam boundary. Discharge: the frozen-seam decision to add `FILLED` (the route SPEC-A7 and ARC 029 Stage 2.2 both took, once the emitting mechanism exists — and `fills.py`'s own docstring already records the §9 `filled` row as OWED), then a `filled` booking on the origin-write path and a gate over it. | ARC 039 | limiter |
| D3.375 | **No standing `checks/check_*.py` reddens when §14:970's structural control is deleted; only a pytest suite does** | ARC 038 (sub-agent C, gate audit P4), MEASURED | `ExecutionReport.__post_init__`'s `filled_qty <= 0` refusal (`execution.py:184`) is the one control that makes a placement ack UNREPRESENTABLE as a fill — the exact negation of §14:970. Deleting it (proven live in the same breath: `ExecutionReport(..., filled_qty=0, ...)` constructed, `ExecutionLedger.ingest` accepted it, `fill_count=1`) left `check_execution_ledger`, `check_fill_handler` and `check_fill_seam` ALL rc=0; restore byte-identical (`sha256 d78bf197…`). `scripts/tests/test_execution.py` DID redden — `2 failed, 108 passed`, naming `test_a_report_with_impossible_QUANTITIES_is_refused_at_construction[0-0-filled_qty=0]` — so the invariant is mechanically guarded, by a suite and not by a gate. Recorded, deliberately NOT discharged with a new check: doctrine C.9 forbids a second instrument re-asserting what `test_execution.py` already asserts, and adding one would be the duplicate-instrument defect. Owed: the ruling on whether an invariant guarded only by pytest counts as gate-covered for the Limiter's badge, and if not, a value-refusal arm inside `check_execution_ledger` rather than a new check file. This is the general question the artifact-gate-coverage ceiling does not currently ask: coverage is measured per ARTIFACT, not per INVARIANT. | ARC 039 | limiter |
